apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: bookstore
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongo

